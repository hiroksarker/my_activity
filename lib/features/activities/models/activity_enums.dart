enum ActivityType {
  expense,
  income,
}

enum ActivityStatus {
  active,
  completed,
  archived,
}

enum TransactionType {
  expense,
  income,
}

enum RecurrenceType {
  daily,
  weekly,
  monthly,
  yearly,
}

enum ActivityPriority {
  low,
  regular,
  high,
  urgent,
}

// Category types
enum CategoryType {
  personal,
  work,
  others,
} 