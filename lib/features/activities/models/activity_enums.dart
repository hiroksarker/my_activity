enum ActivityType {
  task,
  expense,
  income,
}

enum ActivityStatus {
  active,
  completed,
  cancelled,
  archived,
}

enum TransactionType {
  debit,
  credit,
}

enum TaskStatus {
  pending,
  inProgress,
  completed,
  cancelled,
} 