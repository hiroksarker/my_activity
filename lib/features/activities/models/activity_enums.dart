enum ActivityType {
  expense,
  income,
}

enum ActivityStatus {
  active,
  completed,
  archived,
}

enum TransactionType {
  expense,
  income,
}

enum RecurrenceType {
  daily,
  weekly,
  monthly,
  yearly,
}

enum ActivityPriority {
  low,
  regular,
  high,
  urgent,
  default_,
  important,
}

enum TaskStatus {
  pending,
  inProgress,
  completed,
  cancelled,
}

// Category types
enum CategoryType {
  personal,
  work,
  others,
} 